---
import { Code, TabItem, Tabs } from "@astrojs/starlight/components";
interface Props {
	command: string;
}

const { command } = Astro.props;
const biomePackage = "@biomejs/biome";
const biomeBin = "biome";
---

<Tabs syncKey="command">
	<TabItem label="npm" icon="seti:npm">
		<Code code={`npx ${biomePackage} ${command}`} lang="bash"  />
	</TabItem>
	<TabItem label="yarn" icon="seti:yarn">
		<Code code={`yarn exec ${biomeBin} ${command}`} lang="bash"  />
	</TabItem>
	<TabItem label="pnpm" icon="pnpm">
		<Code code={`pnpm exec ${biomeBin} ${command}`} lang="bash"  />
	</TabItem>
	<TabItem label="bun" icon="bun">
		<Code code={`bunx --bun ${biomeBin} ${command}`} lang="bash"  />
	</TabItem>
	<TabItem label="deno" icon="deno">
		<Code code={`deno run -A npm:${biomePackage} ${command}`} lang="bash"  />
	</TabItem>
</Tabs>
