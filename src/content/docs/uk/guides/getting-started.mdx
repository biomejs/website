---
title: Початок роботи
description: Дізнайтеся, як налаштувати новий проект з Biome.
---

import DefaultConfiguration from "@/components/generated/DefaultConfiguration.mdx";
import PackageManagerBiomeCommand from "@/components/PackageManagerBiomeCommand.astro";
import PackageManagerCommand from "@/components/PackageManagerCommand.astro";

## Встановлення

Найшвидший спосіб завантажити Biome - це використати менеджер пакетів, такий як `npm`.
Це вимагає Node.js v14.18 або новішої версії.
CLI також доступний як [окремий виконуваний файл](/guides/manual-installation), якщо ви хочете використовувати Biome без встановлення Node.js.

Щоб встановити Biome, виконайте наступні команди в директорії, що містить файл `package.json`.

<PackageManagerCommand
  npm="install --save-dev --save-exact @biomejs/biome"
  pnpm="add --save-dev --save-exact @biomejs/biome"
  yarn="add --dev --exact @biomejs/biome"
  bun="add --dev --exact @biomejs/biome"
  deno="add --dev npm:@biomejs/biome"
/>

:::note
Ми інструктуємо менеджер пакетів закріпити точну версію Biome.
Це гарантує, що всі в проекті використовують точно ту ж версію Biome.
Навіть випуск патчу може призвести до незначних змін у поведінці.
Дивіться [сторінку версіонування](/internals/versioning/) для отримання додаткової інформації.
:::

## Конфігурація

Ми рекомендуємо створити конфігураційний файл `biome.json` або `biome.jsonc` для кожного проекту.
Це усуває необхідність повторювати параметри CLI кожного разу, коли ви запускаєте команду,
і гарантує, що Biome використовує ту ж конфігурацію у вашому редакторі.
Деякі параметри також доступні лише з конфігураційного файлу.
Якщо вас влаштовують налаштування за замовчуванням Biome, вам не потрібно створювати конфігураційний файл.
Щоб створити файл `biome.json`, виконайте команду `init` у кореневій папці вашого проекту:

<PackageManagerBiomeCommand command="init"/>

Додайте опцію `--jsonc`, щоб створити файл `biome.jsonc`.

Після виконання команди `init` у вашій директорії з'явиться новий файл `biome.json`:

<DefaultConfiguration/>

Параметр `linter.enabled: true` вмикає лінтер, а `rules.recommended: true` вмикає [рекомендовані правила](/linter/rules/).
Це відповідає налаштуванням за замовчуванням.

Форматування увімкнено **за замовчуванням**, але ви можете [вимкнути](/reference/configuration/#formatterenabled) його, явно використовуючи `formatter.enabled: false`.

## Використання

CLI Biome має багато команд і параметрів, тому ви можете використовувати лише те, що вам потрібно.

Ви можете форматувати файли та директорії, використовуючи команду [`format`](/reference/cli#biome-format) з опцією `--write`:

<PackageManagerBiomeCommand command="format --write <files>"/>

Ви можете лінтити та застосовувати [безпечні виправлення](/linter#safe-fixes) до файлів та директорій, використовуючи команду [`lint`](/reference/cli#biome-lint) з опцією `--write`:

<PackageManagerBiomeCommand command="lint --write <files>"/>

Ви можете виконати **обидві** команди, використовуючи команду [`check`](/reference/cli#biome-check):

<PackageManagerBiomeCommand command="check --write <files>"/>

Команда `check` запускає кілька інструментів одночасно.
Вона форматуватиме, лінтитиме та організовуватиме імпорти.

## Встановлення плагіна для редактора

Ми рекомендуємо встановити плагін для редактора, щоб отримати максимальну користь від Biome.
Перегляньте [сторінку редактора](/guides/integrate-in-editor), щоб дізнатися, які редактори підтримують Biome.

## Налаштування CI

Якщо ви використовуєте Node.js, рекомендований спосіб запуску Biome у CI - це використання [вашого улюбленого менеджера пакетів](/guides/getting-started#installation).
Це гарантує, що ваш CI конвеєр використовує ту ж версію Biome, що і ви у редакторі або при запуску локальних команд CLI.
Альтернативно, ви можете використовувати спеціальну [CI Action](/recipes/continuous-integration).

## Наступні кроки

Успіх! Тепер ви готові використовувати Biome. 🥳

- [Міграція з ESLint та Prettier](/guides/migrate-eslint-prettier)
- Дізнайтеся більше про те, як [налаштувати Biome](/guides/configure-biome)
- Дізнайтеся більше про те, як використовувати та налаштовувати [форматувальник](/formatter)
- Дізнайтеся більше про те, як використовувати та налаштовувати [лінтер](/linter)
- Ознайомтеся з [параметрами CLI](/reference/cli)
- Ознайомтеся з [параметрами конфігурації](/reference/configuration)
- Приєднуйтесь до нашої [спільноти на Discord](https://biomejs.dev/chat)

