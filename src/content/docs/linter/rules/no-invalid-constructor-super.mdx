---
title: noInvalidConstructorSuper
description: |
  'Prevents the incorrect use of <code>super()</code> inside classes. It also checks whether a call <code>super()</code> is missing from classes that extends other constructors.'
---
import ContentJs from "@/components/generated/rules/no-invalid-constructor-super_js.md"
import { Tabs, TabItem } from '@astrojs/starlight/components';

**Diagnostic Category: `lint/correctness/noInvalidConstructorSuper`**
<Tabs>
<TabItem label="JavaScript (and super languages)"><ContentJs /></TabItem>
</Tabs>

