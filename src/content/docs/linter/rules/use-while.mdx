---
title: useWhile
description: |
  'Enforce the use of <code>while</code> loops instead of <code>for</code> loops when the initializer and update expressions are not needed.'
---
import ContentJs from "@/components/generated/rules/use-while_js.md"
import { Tabs, TabItem } from '@astrojs/starlight/components';

**Diagnostic Category: `lint/style/useWhile`**
<Tabs>
<TabItem label="JavaScript (and super languages)"><ContentJs /></TabItem>
</Tabs>

