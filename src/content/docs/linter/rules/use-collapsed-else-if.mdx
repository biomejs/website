---
title: useCollapsedElseIf
description: |
  'Enforce using <code>else if</code> instead of nested <code>if</code> in <code>else</code> clauses.'
---
import ContentJs from "@/components/generated/rules/use-collapsed-else-if_js.md"
import { Tabs, TabItem } from '@astrojs/starlight/components';

**Diagnostic Category: `lint/style/useCollapsedElseIf`**
<Tabs>
<TabItem label="JavaScript (and super languages)"><ContentJs /></TabItem>
</Tabs>

