---
title: useTrimStartEnd
description: |
  'Enforce the use of <code>String.trimStart()</code> and <code>String.trimEnd()</code> over <code>String.trimLeft()</code> and <code>String.trimRight()</code>.'
---
import ContentJs from "@/components/generated/rules/use-trim-start-end_js.md"
import { Tabs, TabItem } from '@astrojs/starlight/components';

**Diagnostic Category: `lint/nursery/useTrimStartEnd`**
<Tabs>
<TabItem label="JavaScript (and super languages)"><ContentJs /></TabItem>
</Tabs>

