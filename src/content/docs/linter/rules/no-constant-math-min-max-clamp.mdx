---
title: noConstantMathMinMaxClamp
description: |
  'Disallow the use of <code>Math.min</code> and <code>Math.max</code> to clamp a value where the result itself is constant.'
---
import ContentJs from "@/components/generated/rules/no-constant-math-min-max-clamp_js.md"
import { Tabs, TabItem } from '@astrojs/starlight/components';

**Diagnostic Category: `lint/correctness/noConstantMathMinMaxClamp`**
<Tabs>
<TabItem label="JavaScript (and super languages)"><ContentJs /></TabItem>
</Tabs>

