---
title: noNegationElse
description: |
  'Disallow negation in the condition of an <code>if</code> statement if it has an <code>else</code> clause.'
---
import ContentJs from "@/components/generated/rules/no-negation-else_js.md"
import { Tabs, TabItem } from '@astrojs/starlight/components';

**Diagnostic Category: `lint/style/noNegationElse`**
<Tabs>
<TabItem label="JavaScript (and super languages)"><ContentJs /></TabItem>
</Tabs>

