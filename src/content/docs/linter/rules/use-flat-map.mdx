---
title: useFlatMap
description: |
  'Promotes the use of <code>.flatMap()</code> when <code>map().flat()</code> are used together.'
---
import ContentJs from "@/components/generated/rules/use-flat-map_js.md"
import { Tabs, TabItem } from '@astrojs/starlight/components';

**Diagnostic Category: `lint/complexity/useFlatMap`**
<Tabs>
<TabItem label="JavaScript (and super languages)"><ContentJs /></TabItem>
</Tabs>

