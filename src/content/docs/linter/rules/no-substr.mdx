---
title: noSubstr
description: |
  'Enforce the use of <code>String.slice()</code> over <code>String.substr()</code> and <code>String.substring()</code>.'
---
import ContentJs from "@/components/generated/rules/no-substr_js.md"
import { Tabs, TabItem } from '@astrojs/starlight/components';

**Diagnostic Category: `lint/nursery/noSubstr`**
<Tabs>
<TabItem label="JavaScript (and super languages)"><ContentJs /></TabItem>
</Tabs>

