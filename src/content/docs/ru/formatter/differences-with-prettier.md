---
title: Отличия от Prettier
description: Подробное объяснение отличий от Prettier.
---

В некоторых случаях Biome намеренно решил форматировать код не так, как это делает Prettier. Эти расхождения объясняются ниже.

## Prettier не удаляет кавычки у некоторых свойств объектов, которые являются валидными идентификаторами JavaScript.

Prettier и Biome удаляют кавычки у свойств объектов и классов, которые являются валидными идентификаторами JavaScript.
Prettier [удаляет кавычки только у валидных ES5 идентификаторов](https://github.com/prettier/prettier/blob/a5d502513e5de4819a41fd90b9be7247146effc7/src/language-js/utils/index.js#L646).

Это устаревшее ограничение в экосистеме, где ES2015 теперь широко распространен.
Таким образом, мы решили расходиться здесь, удаляя кавычки у всех валидных идентификаторов JavaScript в ES2015+.

Возможным обходным путем было бы введение конфигурации для установки версии ECMAScript, которую использует проект.
Затем мы могли бы настроить поведение удаления кавычек на основе этой версии.
Установка версии ECMAScript в `ES5` могла бы соответствовать поведению Prettier.

```js title="example.js"
const obj = {
 'a': true,
 b: true,
 "𐊧": true,
}
```

Diff

```js title="example.js" del={4} ins={5}
const obj = {
  a: true,
  b: true,
  "𐊧": true,
  𐊧: true,
};
```


## Prettier имеет непоследовательное поведение для присваивания в вычисляемых ключах.

Prettier и Biome заключают некоторые выражения присваивания в круглые скобки, особенно в условных выражениях.
Это позволяет Biome идентифицировать выражение, которое должно быть сравнением.

Prettier имеет непоследовательное поведение, потому что добавляет круглые скобки для присваивания в вычисляемом ключе свойства объекта и не добавляет для вычисляемого ключа свойства класса, как показано в следующем примере:

Input

```js title="example.js"
a = {
  [x = 0]: 1,
}

class C {
  [x = 0] = 1
}
```

Diff

```js title="example.js" del={2} ins={3}
a = {
  [(x = 0)]: 1,
  [x = 0]: 1,
};

class C {
  [x = 0] = 1;
}
```

[Playground link](/playground?enabledLinting=false&code=YQAgAD0AIAB7AAoAIAAgAFsAeAAgAD0AIAAwAF0AOgAgADEALAAKAH0ACgAKAGMAbABhAHMAcwAgAEMAIAB7AAoAIAAgACAAIABbAHgAIAA9ACAAMABdACAAPQAgADEACgB9AAoA)

Для последовательности мы решили расходиться и опускать круглые скобки.
В качестве альтернативы мы могли бы заключать любое присваивание в вычисляемом ключе объекта или класса.


## Prettier добавляет завершающую запятую к параметрам типа стрелочных функций, даже когда это не требуется.

В некоторых специфических случаях список параметров типа стрелочной функции требует завершающей запятой, чтобы отличить его от JSX элемента.
Когда указан тип по умолчанию, эта завершающая запятая не требуется.
Здесь мы расходимся с Prettier, потому что считаем, что это лучше соответствует первоначальному замыслу Prettier, который заключался в добавлении завершающей запятой только при необходимости.

Input

```tsx title="example.tsx"
<T = unknown>() => {};
```

Diff

```tsx title="example.tsx" del={1} ins={2}
<T = unknown,>() => {};
<T = unknown>() => {};
```


## Prettier имеет непоследовательное поведение для заключенных в круглые скобки опциональных цепочек с non-null-утверждением

В TypeScript оператор non-null-утверждения `!` позволяет утверждать, что значение не является null.
При применении к опциональной цепочке утверждение применяется ко всей цепочке независимо от наличия круглых скобок,
делая эквивалентными `(a.?.b)!` и `a.?.b!`.

Предыдущие примеры кода уже хорошо отформатированы, согласно Prettier.
Prettier используется для обеспечения наличия или отсутствия круглых скобок.
Это выглядит как упущенная возможность нормализовать код.

Более того, Prettier не удаляет круглые скобки, даже когда они заключают non-null-утверждение.
Вместо этого он перемещает оператор за пределы круглых скобок.

Input:

```ts title="example.ts"
a.?.b!
(a.?.b)!
(a.?.b!)
```

Diff

```ts title="example.ts" del={2, 4} ins={3, 5}
a.?.b!
(a.?.b)!
a.?.b!
(a.?.b!)
a.?.b!
```


## Prettier форматирует невалидный синтаксис

Парсинг JavaScript и TypeScript в Prettier на основе Babel очень либеральный и [позволяет игнорировать множество ошибок](https://github.com/prettier/prettier/blob/e4a74c05f4502dd4ec70495c3130ff08ab088e05/src/language-js/parse/babel.js#L177-L218).
Парсер Biome намеренно более строгий, чем парсер Prettier.
Он корректно идентифицирует следующие синтаксические ошибки:

- функция не может иметь дублирующиеся модификаторы
- неверный порядок модификаторов свойств
- объявления функций не могут иметь тела
- неабстрактные классы не могут иметь абстрактные свойства
- опциональной цепочке нельзя присвоить значение
- модификатор `const` не может быть установлен для параметра типа интерфейса
- return на верхнем уровне
- и т.д.

В Prettier эти ошибки не считаются ошибками парсинга, и AST все равно строится "корректно" с соответствующими узлами.
При форматировании Prettier обрабатывает эти узлы как обычные и форматирует их соответствующим образом.

В Biome ошибки парсинга приводят к появлению узлов `Bogus`, которые могут содержать любое количество валидных узлов, невалидных узлов и/или необработанных символов.
При форматировании Biome обрабатывает bogus-узлы как фактически простой текст, выводя их дословно в результирующий код без какого-либо форматирования, поскольку попытка их форматирования может быть неверной и вызвать семантические изменения.

Для свойств классов текущая стратегия парсинга Prettier также использует булевы поля для модификаторов, что означает, что может присутствовать только один модификатор каждого вида (модификаторы доступности хранятся как одна строка).
При выводе Prettier смотрит на список булевых значений и решает, какие модификаторы снова вывести. Biome вместо этого хранит список модификаторов, что означает, что дубликаты сохраняются и могут быть проанализированы (отсюда сообщения об ошибках парсинга о дублирующихся модификаторах и порядке).
При выводе bogus-узлов этот список остается нетронутым, и вывод неотформатированного текста приводит к тому, что эти модификаторы продолжают существовать.

Есть способы, которыми Biome может решить эту проблему.
Одна возможность - попытаться интерпретировать узлы Bogus при форматировании и создать из них валидные узлы.
Если валидный узел может быть построен, то он просто отформатирует этот узел как обычно, в противном случае выведет bogus-текст дословно, как это делается в настоящее время.
Однако это беспорядочно и вносит форму логики парсинга в форматировщик, которая не имеет смысла.

Другой вариант - ввести какую-то форму "синтаксически валидного bogus-узла" в парсер, который принимает эти виды чисто семантических ошибок (дублирующиеся модификаторы, абстрактные свойства в неабстрактных классах).

Он продолжал бы строить узлы как обычно (фактически соответствуя поведению Prettier), но хранил бы их внутри нового вида bogus-узла, включая диагностику вместе с ним.
При форматировании эти конкретные bogus-узлы просто попытались бы отформатировать внутренний узел, а затем вернулись бы к исходному, если возникла ошибка (существующая утилита `format_or_verbatim` уже это делает).
Это сохраняет логику парсинга и форматирования отдельно друг от друга, но добавляет больше сложности в парсер, позволяя невалидным состояниям считаться полувалидными.

### Дублирующиеся модификаторы на свойствах классов

Input

```ts title="example.ts"
// Multiple accessibility modifiers
class Foo {
  private public a  = 1;
}

// Declare function with body
declare function foo ( ) {  }

// Invalid use of abstract
class Bar {
  abstract  foo  ;
}

// Duplicate Readonly
class Read {
  readonly readonly   x: number;
}
```

Diff

```ts title="example.ts" del={3, 8, 13, 19} ins={4, 9, 14, 20}
// Multiple accessibility modifiers
class Foo {
  private public a  = 1;
  private a = 1;
}

// Declare function with body
declare function foo ( ) {  }
declare function foo() {};

// Invalid use of abstract
class Bar {
  abstract  foo  ;
  abstract foo;
}

// Duplicate Readonly
class Read {
  readonly readonly   x: number;
  readonly x: number;
}
```

### Присваивание опциональной цепочке

Input

```js title="example.js"
(a?.b) = c;
```

Diff

```js title="example.js" del={1} ins={2}
a?.b = c;
(a?.b) = c;
```

### Неверный модификатор для параметров типа интерфейса

Input

```ts title="example.js"
interface L<in const T> {}
```

Diff

```ts title="example.js" del={1} ins={2}
interface L<const in T> {}
interface L<in const T> {}
```

### Return на верхнем уровне

```js title="example.js"
return someVeryLongStringA && someVeryLongStringB && someVeryLongStringC && someVeryLongStringD
```

```js title="example.js" del={2, 3, 4, 5, 6, 7} ins={1}
return someVeryLongStringA && someVeryLongStringB && someVeryLongStringC && someVeryLongStringD
return (
  someVeryLongStringA &&
  someVeryLongStringB &&
  someVeryLongStringC &&
  someVeryLongStringD
);
```

### Ошибочные self-increment и self-decrement

Input

```js title="example.js"
(1)++;
```

```js title="example.js" del={1} add={2}
1++;
(1)++;
```

### Использование модификатора `abstract` в неабстрактных классах

Input

```ts title="example.js"
class C {
  abstract f() : number;
}
```

Diff


```ts title="example.js" del={2} add={3}
class C {
  abstract f(): number;
  abstract f() : number;
}
```

## Prettier имеет несоответствия между парсингом TypeScript и Babel

Prettier поддерживает несколько различных парсеров для JavaScript и TypeScript кода, все из которых должны быть совместимы со [спецификацией `estree`](https://github.com/estree/estree). В большинстве случаев Prettier использует Babel как парсер по умолчанию для JavaScript кода, но при парсинге TypeScript он сначала попытается использовать собственный парсер TypeScript и только потом вернется к Babel с включенным TypeScript. Хотя парсер TypeScript в целом совместим с `estree`, он не точен, и [это может привести к некоторым несоответствиям](https://github.com/prettier/prettier/issues/15785), которые влияют на вывод, создаваемый Prettier. В целом, это считается багами в самом Prettier, поскольку вывод должен быть одинаковым независимо от используемого парсера.

Biome реализует собственный парсинг, который обрабатывает все формы JavaScript и TypeScript кода, что означает, что не должно быть никаких несоответствий между ними. Однако при миграции кодовой базы TypeScript с Prettier на Biome возможно, что некоторое форматирование будет выглядеть измененным из-за этих расхождений между парсерами Prettier.

Эти случаи не считаются багами или несовместимостями в Biome. Если отформатированный код выглядит по-разному только при использовании настройки парсера `typescript` в Prettier, но совпадает при использовании `babel` и/или `babel-ts`, то Biome считает вывод совместимым.

В качестве примера рассмотрим этот случай, отформатированный с помощью Biome и Prettier 3.1.0 с парсером `typescript`:

Input

```ts title="example.js"
function someFunctionName(
  someLongBreakingParameterName,
  anotherLongParameterName,
) {
  return isEqual(a?.map(([t, _]) => t?.id), b?.map(([t, _]) => t?.id));
}
```

Diff


```ts title="example.js" del={5} ins={6,7,8,9}
function someFunctionName(
  someLongBreakingParameterName,
  anotherLongParameterName,
) {
  return isEqual(a?.map(([t, _]) => t?.id), b?.map(([t, _]) => t?.id));
  return isEqual(
    a?.map(([t, _]) => t?.id),
    b?.map(([t, _]) => t?.id),
  );
}
```

Prettier с парсером TypeScript выбирает записать вызов `isEqual` в одну строку, в то время как Biome соответствует выводу Prettier с парсерами `babel` и `babel-ts`. Таким образом, это не считается несовместимостью с Biome и вместо этого считается багом в Prettier.
